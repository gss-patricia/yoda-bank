import { AnyAction } from "redux";
import UserActions from "../actions/UserActions";

const { CREATE } = UserActions;
const userReducers: any = 0; //TODO: Implementar os reducers
export default userReducers;
